# Core app
streamlit>=1.36
pandas>=2.1
numpy>=1.26

# Enrichment engines
# Cap retained deliberately. _compact_raw and the running plots reach into
# gseapy internals (see _GSEAPY_HEAVY_ATTRS), so a major bump needs
# test_run_prerank_real_smoke to pass first. Verified 2026-08-13 on 1.1.12 and
# on 1.3.1, which is what the deployed root requirements.txt resolves to.
gseapy>=1.1,<2
gprofiler-official>=1.0

# Plotting
plotly>=5.22
matplotlib>=3.8
networkx>=3.1
statsmodels>=0.14

# Testing
pytest>=7.0
